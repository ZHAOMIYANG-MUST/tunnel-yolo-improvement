# 数据分析报告：目标形态统计与改进动机

> 数据来源：datasets/tunnel（train+val，共 2510 个渗漏目标框）
> 统计脚本：scripts/analyze_dataset.py；图表：analysis/target_statistics.png

## 一、核心发现

| 统计项 | 结果 | 含义 |
|---|---|---|
| 目标面积 | **70.8% 为大目标**（归一化面积 ≥0.1），小目标仅 2% | 渗漏区域整体偏大，"小目标"不是本数据集的主要矛盾 |
| 高度分布 | **高中位数 0.749，23% 的框 h≈1.0**（贯穿整图高度） | 渗漏形态以**纵向水渍长条**为主，沿衬砌表面竖向延伸 |
| 宽度分布 | 宽中位数 0.281，集中于 0.05~0.35 | 单条水渍较窄——"窄"而非"小" |
| 长宽比 | **38.9% 的框长宽比 ≥3 或 ≤1/3**（极瘦长/极扁宽） | 病态长宽比目标占比近四成 |
| 每图目标数 | 均值 1.19，85.2% 的图只有 1 个目标 | 场景稀疏，无密集小目标问题 |

## 二、对原改进计划的修正

原计划（P2 小目标层 + 注意力 + 损失改进）基于领域常见假设，但本数据集实测后应调整为：

1. **损失函数改进（WIoU/Shape-IoU）—— 动机最强，列为核心创新**。38.9% 的极瘦长目标正是 CIoU 类损失的盲区（对形状不敏感、对细长目标回归慢），实测数据为自设计损失提供了直接依据。
2. **注意力机制 —— 保留**。隧道低光照、水渍纹理与背景对比度低的问题依然存在（可通过 Grad-CAM 验证）。
3. **P2 小目标层 —— 降级为可选**。小目标仅占 2%，P2 的原始动机不足；若保留应改述为"增强窄条目标的特征分辨率"（水渍宽度窄，下采样 8 倍后仅剩约 10~40 像素宽，仍有信息损失），或改用其他针对性设计（如保持高分辨率的颈部结构）。
4. **（新增候选）纵横比感知设计**：可考虑在标签分配或框回归中引入对瘦长形状的自适应处理，作为区别于"堆模块"的自设计创新点。

## 三、论文表述建议

数据分析结论可直接写进论文 3.1 节（改进动机）："对数据集 2510 个渗漏标注框的统计表明，70.8% 的目标为大面积区域，38.9% 的目标长宽比超出 [1/3, 3] 区间，且 23% 的目标纵向贯穿整幅图像，呈现典型的细长形态。标准 IoU 损失对细长目标的尺度敏感性不足……"——先给数据，再引出改进，逻辑闭环。

## 四、遗留验证项

- [ ] 三类数据集（crack/leak/spalling）购入后重跑本分析——裂缝类预期以细小/低对比目标为主，届时 P2 动机可能重新成立
- [ ] 基线漏检样本可视化（需要跑一遍测试集推理并对比标注，待做）
