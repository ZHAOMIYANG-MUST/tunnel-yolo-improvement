# 隧道病害检测毕业设计·完整执行手册

> 适用：人工智能专业本科毕设
> 题目建议：《基于改进 YOLOv8 的小目标检测算法研究——以隧道衬砌病害检测为例》
> 检测目标：渗漏水（leak）、裂缝（crack）、剥落（spalling）三类隧道衬砌病害

---

## 阶段一：文献调研与开题（第 1-3 周）

### 1.1 必读文献清单（按顺序）

| 序号 | 文献 | 作用 |
|---|---|---|
| 1 | Redmon et al.《You Only Look Once》(CVPR 2016) | YOLO 起源，绪论必引 |
| 2 | YOLOv8 / Ultralytics 官方文档 (docs.ultralytics.com) | 基线框架 |
| 3 | Tunnel-YOLO (Engineering Applications of AI, 2025) | 盾构隧道渗漏水检测改进范本 |
| 4 | Tunnel-YOLO (Transportation Research Record, 2025) | 隧道车辆检测改进范本 |
| 5 | 清华学报《基于YOLO框架的隧道衬砌表观病害智能识别》(LADDNet) | 同任务最新中文参考 |
| 6 | CBAM (ECCV 2018)、SimAM (ICML 2021)、BiFPN (EfficientDet) | 注意力与多尺度融合模块出处 |
| 7 | WIoU / Shape-IoU 损失函数论文 | 损失改进出处 |
| 8 | 知网搜"隧道 衬砌 病害 YOLO"，挑 5 篇近年硕士论文 | 学习论文结构与图表规范 |

### 1.2 开题报告结构（直接套用）

1. **选题背景与意义**：人工巡检效率低、危险性高 → 深度学习自动检测的必要性（2 页）
2. **国内外研究现状**：传统方法 → 深度学习方法 → YOLO 系列改进现状（3 页）
3. **研究内容**：基线复现 + 改进点设计 + 消融验证（1 页，写 3 条）
4. **技术路线**：一张流程图（数据 → 基线 → 改进 → 实验 → 结论）
5. **进度安排**：按周列甘特图
6. **参考文献**：15~20 篇，中英各半

---

## 阶段二：环境搭建（第 6 周，可与数据准备并行）

### 2.1 硬件方案（三选一）

- 有 NVIDIA 显卡（≥6GB 显存）：本地训练，最方便
- 没有 GPU：Google Colab 免费版（T4 显卡）或 Kaggle（每周 30h GPU）
- 折中：AutoDL / 恒源云租卡，约 ¥1/小时，RTX 3090

### 2.2 环境安装命令

```bash
# 1. 创建虚拟环境（Python 3.10）
conda create -n tunnel python=3.10 -y
conda activate tunnel

# 2. 安装 PyTorch（CUDA 版，本地有 N 卡时）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# 3. 安装 Ultralytics
pip install ultralytics

# 4. 验证
yolo predict model=yolov8n.pt source='https://ultralytics.com/images/bus.jpg'
```

### 2.3 项目目录结构（从第一天就保持规范）

```
tunnel-graduation/
├── datasets/            # 数据集
│   └── tunnel/
│       ├── images/{train,val,test}
│       └── labels/{train,val,test}
├── configs/
│   └── tunnel.yaml      # 数据集配置
├── models/              # 改进版模型 yaml
├── runs/                # 训练输出
├── scripts/             # 划分数据、统计等脚本
├── docs/                # 实验记录
└── train.py
```

---

## 阶段三：数据准备（第 4-5 周）

### 3.1 数据来源（已核实）

| 数据集 | 规模 | 获取方式 |
|---|---|---|
| 隧道墙壁缺陷数据集（3 类） | 3231 张 / 4593 框 | 面包多平台或 GitHub datasets_sl 仓库 |
| OpenConstruction Tunnel Leakage | 2351 张 | openconstruction.org，网盘下载，GPL-3.0 |
| Crack-Seg 等裂缝数据集 | 补充样本 | 公开下载，扩充裂缝类 |

主用第一个（3 类正好对应你的检测目标），用后两个补充裂缝类样本。

### 3.2 数据集划分脚本（scripts/split_dataset.py）

```python
import random, shutil, os
from pathlib import Path

SRC = Path("raw_dataset")          # 原始数据（含 images/ 和 labels/）
DST = Path("datasets/tunnel")
RATIO = (0.8, 0.1, 0.1)            # 训练/验证/测试

imgs = sorted((SRC / "images").glob("*.jpg"))
random.seed(42)                    # 固定种子，保证可复现
random.shuffle(imgs)

n = len(imgs)
splits = {"train": imgs[:int(n*RATIO[0])],
          "val":   imgs[int(n*RATIO[0]):int(n*(RATIO[0]+RATIO[1]))],
          "test":  imgs[int(n*(RATIO[0]+RATIO[1])):]}

for name, files in splits.items():
    (DST/"images"/name).mkdir(parents=True, exist_ok=True)
    (DST/"labels"/name).mkdir(parents=True, exist_ok=True)
    for f in files:
        shutil.copy(f, DST/"images"/name/f.name)
        lbl = SRC/"labels"/(f.stem + ".txt")
        if lbl.exists():
            shutil.copy(lbl, DST/"labels"/name/lbl.name)
print(f"train={len(splits['train'])}, val={len(splits['val'])}, test={len(splits['test'])}")
```

### 3.3 数据集配置（configs/tunnel.yaml）

```yaml
path: datasets/tunnel
train: images/train
val: images/val
test: images/test

names:
  0: leak       # 渗漏水
  1: crack      # 裂缝
  2: spalling   # 剥落
```

> 注意：类别顺序以数据集自带的 classes.txt 为准，不要照抄。

### 3.4 数据分析（写进论文"数据集构建"一节）

统计并画出：每类标注框数量（类别不均衡程度）、框宽高分布（验证"小目标"论点，这是你改进动机的来源）、样本示例图。一篇脚本即可，论文里至少 2 张图来自这里。

---

## 阶段四：基线复现（第 6-7 周）

### 4.1 训练脚本（train.py）

```python
from ultralytics import YOLO

model = YOLO("yolov8n.pt")     # 预训练权重，迁移学习起点
model.train(
    data="configs/tunnel.yaml",
    epochs=300,
    imgsz=640,
    batch=16,                  # 显存不够就调小
    seed=42,                   # 关键：固定随机种子
    device=0,                  # Colab 上也是 0
    project="runs",
    name="baseline",
)
metrics = model.val()          # 在测试集上评估
print(metrics.box.map)         # 记录 mAP50-95
```

### 4.2 必须记录的基线指标（消融表第一行）

| 指标 | 含义 |
|---|---|
| Precision / Recall | 查准率 / 查全率 |
| mAP@0.5 / mAP@0.5:0.95 | 平均精度，主指标 |
| Params (M) / GFLOPs | 模型大小与计算量 |
| FPS | 推理速度 |

### 4.3 常见坑

- 训练损失为 NaN → 显存不足，batch 调小或 imgsz=512
- 类别不均衡导致小类 mAP 极低 → 正常现象，写进分析，也是你加"数据增强/损失改进"的理由
- 跑一次完整训练前先用 epochs=10 快速验证数据配置无误

---

## 阶段五：模型改进（第 8-12 周，核心阶段）

### 5.1 改进点选择（1 个自设计 + 2 个即插即用）

| 优先级 | 改进点 | 类型 | 针对的问题 | 预期收益 |
|---|---|---|---|---|
| ★★★ | 损失函数改进：WIoU 或自设计的 Shape-IoU 变体 | 自设计/半自设计 | 病害框细长不规则 | 稳定涨点，答辩好讲 |
| ★★★ | 小目标检测层（P2 层） | 结构改进 | 衬砌病害多为小目标 | Recall 明显提升 |
| ★★ | 注意力机制（CBAM/SimAM/SGE） | 即插即用 | 低光照、背景干扰 | 1~3 个点 |
| ★★ | BiFPN 特征融合 | 即插即用 | 多尺度目标 | 1~2 个点 |
| ★ | 轻量化（GhostNet 骨干） | 可选 | 部署需求 | 论文加分项 |

### 5.2 添加改进模块的标准步骤（以注意力为例）

1. 在 `ultralytics/nn/modules/` 下新建模块文件，实现 `forward`
2. 在 `ultralytics/nn/tasks.py` 注册模块名
3. 复制 `yolov8n.yaml` 为 `models/yolov8n-cbam.yaml`，在 neck 的合适位置插入模块行
4. 训练并对比基线，单独记录

### 5.3 实验节奏（严格遵守）

- 一次只改一个变量：基线 → +注意力 → +P2 → +损失，逐步叠加
- 每组实验固定 seed=42、epochs=300、imgsz=640，其余超参完全一致
- 每个 run 的结果截图 + 复制 results.csv 存入 docs/，防止之后对不上数

---

## 阶段六：实验与可视化（第 11-13 周）

### 6.1 对比实验表（论文核心表）

| Model | P | R | mAP50 | mAP50-95 | Params | GFLOPs | FPS |
|---|---|---|---|---|---|---|---|
| Faster R-CNN | | | | | | | |
| YOLOv5s | | | | | | | |
| YOLOv7-tiny | | | | | | | |
| YOLOv8n（基线） | | | | | | | |
| **本文方法** | | | | | | | |

### 6.2 消融实验表

| # | Baseline | +CBAM | +P2 | +WIoU | mAP50 | 提升 |
|---|---|---|---|---|---|---|
| 1 | ✓ | | | | | — |

### 6.3 必备可视化图

1. 检测结果对比图：基线漏检/误检 vs 本文方法正确检出（挑 4~6 张典型图）
2. PR 曲线（Ultralytics 自动生成，runs/exp/ 下直接取）
3. Grad-CAM 热力图：证明注意力模块关注到了病害区域（答辩大杀器）
4. 训练 loss/mAP 曲线
5. 改进前后的网络结构图（用 draw.io 画，别截图别人的）

---

## 阶段七：论文撰写（第 13-16 周）

### 7.1 章节结构

1. **绪论**：背景意义、国内外现状、本文工作
2. **相关理论**：YOLOv8 架构（backbone/neck/head 逐个讲）、损失函数原理
3. **数据集构建**：数据来源、预处理、增强策略、统计分析
4. **改进算法设计**：每个改进点一小节——动机 → 结构 → 公式 → 在整体网络中的位置
5. **实验与分析**：环境配置、对比实验、消融实验、可视化分析
6. **总结与展望**

### 7.2 写作顺序建议

先写实验章节（数据现成）→ 再写方法章节 → 再写理论 → 最后写绪论和总结。摘要放最后写。

---

## 阶段八：答辩准备

### 必被问到的问题（提前写好答案）

1. 你的创新点是什么，和现有工作的区别？
2. 为什么选 YOLOv8 不选更新的版本/两阶段方法？
3. 消融实验为什么这么设计？涨点是模块带来的还是随机波动？
4. 注意力模块为什么有效？（用 Grad-CAM 图回答）
5. 模型能实际部署吗？（准备轻量化或边缘设备方案一句话）
6. 数据集是自己标注的吗，标注质量如何保证？

### 答辩 PPT 结构（10~15 页）

选题背景（1页）→ 总体思路图（1页）→ 数据集（1页）→ 网络结构+改进点（3页）→ 实验结果（3页）→ 总结（1页）→ 致谢（1页）

---

## 时间总表

| 周 | 任务 | 产出物 |
|---|---|---|
| 1-3 | 文献调研、开题报告 | 开题报告 + 技术路线图 |
| 4-5 | 数据下载、清洗、划分、分析 | tunnel.yaml + 数据统计图 |
| 6-7 | 环境搭建、基线训练 | 基线指标表 |
| 8-10 | 改进点逐个实现与验证 | 各改进模块代码 + 初步结果 |
| 11-13 | 完整消融 + 对比实验 + 可视化 | 实验表格 + 全部论文图 |
| 14-16 | 论文撰写、查重、PPT、答辩 | 论文 + PPT |

---

## 每阶段可随时找我做的事

- 写开题报告初稿 / 技术路线图绘制
- 改进模块的具体代码实现（CBAM、P2 层、WIoU 等直接给你能跑的文件）
- 实验结果分析、表格生成
- 论文章节代写框架与润色
- 网络结构图绘制
